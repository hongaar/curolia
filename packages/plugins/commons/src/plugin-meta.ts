/** Stable ids and labels shared by manifest and pin UI (single source of truth). */
export const commonsPluginMeta = {
  typeId: "commons" as const,
  displayName: "Wikimedia Commons",
  implemented: true as const,
} as const;
