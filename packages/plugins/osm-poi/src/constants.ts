/** Search radius for nearest OSM feature (meters). */
export const OSM_POI_SEARCH_RADIUS_M = 40;

/** Max nearby OSM places returned in the pin editor picker. */
export const OSM_POI_NEARBY_CANDIDATES_LIMIT = 15;

/** Re-fetch when cached payload is older than this (ms). */
export const OSM_POI_CACHE_MAX_AGE_MS = 7 * 24 * 60 * 60 * 1000;

export const OSM_POI_COORD_EPSILON = 0.0001;
