/** Stable ids and labels shared by manifest and UI. */
export const googleMapsSavedListsPluginMeta = {
  typeId: "google_maps_saved_lists" as const,
  displayName: "Google Maps",
  implemented: true as const,
};
