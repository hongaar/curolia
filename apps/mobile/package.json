{
  "name": "@curolia/mobile",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync": "cap sync",
    "sync:android": "cap sync android",
    "sync:ios": "cap sync ios",
    "copy": "cap copy",
    "open:ios": "cap open ios",
    "open:android": "cap open android",
    "dev:android": "cap run android -l --forwardPorts 5173:5173 --port 5173 --host localhost",
    "dev:ios": "cap run ios -l --port 5173 --host localhost"
  },
  "dependencies": {
    "@capacitor/core": "^8.3.1",
    "@capacitor/device": "^8.0.2",
    "@capacitor/geolocation": "^8.0.2",
    "@capacitor/push-notifications": "^8.0.3"
  },
  "devDependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/ios": "^8.3.1"
  }
}
